Legend for Latency Graph
Created: 2013-08-08

1.)-Server calls a function through signalling 10,000 times
-Calling the function signals a single time using display_msg_func, which sends a message (but doesn't do anything with it)

2.)-Calling the function signals a single time using display_msg_func, which sends a message (but doesn't do anything with it)

3.)-Calling the function signals a single time using display_msg_func, which sends a message (but doesn't do anything with it)

4.)-Calling the function signals a single time using display_msg_func, which sends a message (but doesn't do anything with it)

5.)-Calling the function signals a single time using display_msg_func, which sends a message (but doesn't do anything with it)